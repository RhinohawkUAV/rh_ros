# This is legacy.  This solves a purely static path-finding problem.  We may want something like this in the future, but
# this version will probably be replaced.
from staticPathFinder import StaticPathFinder
