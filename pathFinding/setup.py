from setuptools import setup

setup(name='rhinoPathFinding',
      version='0.5',
      description='Path finding code for Rhinohawk project',
      author='Stephen Pheiffer',
      install_requires=[
          'numpy', 'typing',
      ],
      zip_safe=False)
